Title: 支持向量机系列之最大间隔   
Date: 2016-04-19   
slug: svm-1-max-margin   
category: 机器学习   
tags: CS, 机器学习, 人工智能  
Modified: 2016-11-09 11:56   
summary: 支持向量机最大间隔介绍   

[TOC]

# 

{%process
|下面我们开始
@a
@b;`x+y`
@c;a,b;$w^Tx+b=0$
@b;d;![x](http://blog.pluskid.org/wp-content/uploads/2010/09/Hyper-Plane.png)
@d;e;`x/y`
#请把w作为系数写在x前面
#但维度是否对上，没对上要如何对上？#偏置项都是kx+b哪见过kx-b了#答案w^Tx + b = 0
%}

# 导读

{%checkbox|你觉得评估标准应该是什么？&w范数值越小越好&超平面到所有点的距离之和越大越好&超平面到最近点的距离越大越好&w范数越大越好
@超平面到最近点的距离越大越好
#w范数:10x+8 = 0 是否等价于 5x+4=0?,所有点:考虑极端情况，一条线离两个类的几个点非常近，离其他点非常远，效果并不好
%}

# 分类问题定义

{%checkbox|你觉得评估标准应该是什么？&w范数值越小越好&超平面到所有点的距离之和越大越好&超平面到最近点的距离越大越好&w范数越大越好
@a&(b|e)&(f|g)|h
#不知道
%}

{%checkbox|你觉得评估标准应该是什么？&w范数值越小越好&超平面到所有点的距离之和越大越好&超平面到最近点的距离越大越好&w范数越大越好@超平面到最近点的距离越大越好
#w范数:10x+8 = 0 是否等价于 5x+4=0？
#所有点:考虑极端情况，一条线离两个类的几个点非常近，离其他点非常远，效果并不好
%}


### 线性分类器

再假设该超平面的权重系数向量w为 n维列向量 ， bias偏置项为 b， x为$n\times m$维矩阵。

用于醒脑的第二个问题，

{%formula|这个超平面的方程(f(x)=0)表示为_@:w^Tx+b=0#例8y-c=0,=0不能省#请把w作为系数写在x前面#但维度是否对上，没对上要如何对上？%}
